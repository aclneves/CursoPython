# Operadores lógicos
# AND (and), OR (or), NOT (not)
# not - usado para inverter opções
# not True = False
# not False = True

senha = input("Senha: ")

if not senha:
    print("Você não digitou nada")

print(not 0)
print(not False)